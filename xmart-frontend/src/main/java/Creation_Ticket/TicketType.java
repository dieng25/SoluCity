package Creation_Ticket;

import javax.swing.*;

public class TicketType extends JFrame {
    private JButton IncidentButton;
    private JButton SuggestionButton;

    public TicketType() {
        IncidentButton = new JButton("Incident");
        IncidentButton.addActionListener(e -> {})
    }


}
